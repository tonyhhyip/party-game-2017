query GetAttendee($id: String) {
    attendees(id: $id) {
        id
        name
        checkIn
        finishGame
        ticket
        records {
            createTime
            booth {
                name
            }
        }
    }
}