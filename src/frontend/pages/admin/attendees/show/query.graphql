query GetAttendee($id: String) {
    attendees(id: $id) {
        id
        name
        checkIn
        finishGame
        records {
            createTime
            booth {
                name
            }
        }
    }
}